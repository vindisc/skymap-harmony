# HAP 归档记录

更新时间：2026-07-10

本文件只记录已经确认的发布归档。当前 0.1.0 已提交审核但尚未确认通过，因此先保留待补齐记录，不提前创建正式 tag。

## 归档规则

审核通过后再执行：

```bash
git status --short
git rev-parse --short HEAD
git tag -a v0.1.0 -m "发布 0.1.0"
git push origin v0.1.0
```

创建 tag 前必须确认：

- 当前 commit 就是华为审核通过版本对应的源码。
- HAP 已成功签名并与华为后台审核包一致。
- `AppScope/app.json5` 的 `versionName` 和 `versionCode` 与审核包一致。
- `git status --short` 中没有会影响发布源码的未提交改动。
- HAP 文件名、大小、构建时间、签名状态已经记录在下方表格。

## 0.1.0 待归档记录

| 字段 | 内容 |
| --- | --- |
| 状态 | 等待华为审核结果 |
| versionName | `0.1.0` |
| versionCode | `3` |
| 计划 tag | `v0.1.0` |
| 源码 commit | 审核通过后补齐 |
| HAP 文件名 | 审核通过后补齐 |
| HAP 路径 | 审核通过后补齐 |
| HAP 大小 | 审核通过后补齐 |
| 构建时间 | 审核通过后补齐 |
| 签名状态 | 审核通过后补齐 |
| 华为审核结果 | 等待审核 |
| 备注 | 当前不提前创建 tag，避免 tag 指向非最终审核通过版本 |

## 历史归档

暂无。
